package com.telus.spring.ai.config;


import org.springframework.ai.openai.api.OpenAiApi;
import org.springframework.web.client.RestClient;
import org.springframework.web.reactive.function.client.WebClient;

/**
 * Simple implementation of OpenAiApi that uses Fuelix.ai API endpoints.
 */
//public class FuelixOpenAiApi extends OpenAiApi {

    /**
     * Create a new Fuelix API client with the given configuration.
     * 
     * @param baseUrl Fuelix API base URL
     * @param apiKey Fuelix API key
     * @param model Default model to use
     */
//    public FuelixOpenAiApi(String baseUrl, String apiKey, String model) {
//        // Call the parent constructor with the Fuelix configuration
//        super(baseUrl, apiKey);
//    }
//}
